/**
 * join -> join method()  of array crates and returns a new string by concatenating all the elements in the array ;
 * also arr.toString is similar to join() method
 */

let arr = [1,2,3,4,5];

console.log(arr.join()); 

console.log(arr.join(" "));

console.log(arr.join("-"))